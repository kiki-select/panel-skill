# 命名 / 空间 / description 治理规范

建 panel/dashboard 是往 BI 平台写**持久化资产**，会被团队长期看到。脏看板的危害比一次性错 CSV 大得多
（长期误导决策、污染检索）。本技能对治理强约束。

## 1. 防 prod 污染

- 在正式服（如 app_id=42）建报表前，先确认是**正式沉淀**还是**测试**。
- **测试/AI 临时产物**：标题加显式前缀（`AI测试-`），用完即删，别留在正式空间。
- **正式沉淀**：遵循下方目录标准命名（`主题-指标-粒度`），**不强加 `AI-` 前缀**——以便直接归入现有目录、与人工报表统一；来源通过 description 标注即可。
- 新建看板默认落在**当前用户私有空间**，不会直接进公开业务空间 —— 要进公开空间需人工搬运。

## 2. 命名规范（必须与《香肠派对》数据看板目录统一）

**权威来源**：飞书电子表格《香肠派对》数据看板目录
（https://sofunny.feishu.cn/wiki/EHXXwKlPSiksmMkaO7qc6HBNnVd ，spreadsheet_token `Tturs6uhlh5BeotQcw1cejzdnMb`）。
新建/调整命名前应先对照它，尤其「总目录」页签的 **「调整后名称」列**就是标准命名。

> 读取方式（需本机已 `lark-cli` 登录飞书）：
> ```bash
> # 列页签
> MSYS_NO_PATHCONV=1 lark-cli api GET "/open-apis/sheets/v3/spreadsheets/Tturs6uhlh5BeotQcw1cejzdnMb/sheets/query"
> # 读总目录单元格（H 列=调整后名称）
> MSYS_NO_PATHCONV=1 lark-cli api GET "/open-apis/sheets/v2/spreadsheets/Tturs6uhlh5BeotQcw1cejzdnMb/values/e9a64f!A1:H80"
> ```
> （Windows Git Bash 必须 `MSYS_NO_PATHCONV=1`，否则 `/open-apis/...` 路径会被 MSYS 篡改成 404。）

### 目录层级（4 级）

`一级目录 → 二级目录 → 三级目录(看板) → 具体面板(panel)`。前三级是业务分类骨架，第四级才是我们建的 panel。命名时让 panel 能**对号入座**到对应业务域。

| 一级目录(部分) | 覆盖 |
|---|---|
| 香肠整体数据 | 活跃/新增/留存/付费/参与度/模式参与/摸金匹配/摸金OKR |
| 摸金派对 | 摸金活跃/流失/养成/经济/留存/参与/对局/战斗/匹配… |
| 玩法系统 | 经典派对/街机派对各玩法、身份卡、枪械、载具、BR… |
| 活跃系统 / 付费系统 / 外围系统 / 安全运营 | 见目录 |

### 命名结构（按「调整后名称」列归纳）

**`<主题/模块>-<指标/对象>[-<粒度/限定>]`**，分隔符统一用 `-`。真实示例：

| 调整后名称 | 拆解 |
|---|---|
| `活跃-同时在线-5分钟趋势` | 主题 活跃 / 对象 同时在线 / 粒度 5分钟趋势 |
| `活跃-分时DAU-逐时累计` | |
| `留存-新增账号留存率` | 无粒度后缀也可 |
| `付费-大盘概况-逐日` | |
| `模式参与-经典派对-逐日` | |
| `摸金经济-货币产销-活跃玩家` | 末段也可是人群/细分 |
| `摸金战斗-地图KD分布` | |

### 标准词汇（不要自造同义词）

| 类别 | 标准叫法（用这些） | 禁止 |
|---|---|---|
| 模式 | **经典派对 / 摸金派对 / 街机派对** | 经典模式 / 撤离模式 / 摸金 / 街机模式 |
| 时间粒度 | 逐日 / 逐时累计 / 5分钟趋势 / 按周 | 每天 / 按日 / 趋势图 |
| 人群 | 活跃玩家 / 新进玩家 / 回流玩家 | 老玩家 / 新用户 |

- 禁止：`测试`、`新报表`、`panel1` 这类无信息名。
- 拿不准主题/模块叫法时，去「总目录」找最接近的现有 panel 对齐。

## 3. description 必填（与 ds 看板检索闭环）

**这是本技能的硬约束**。ds-skill 检索看板已改为「description-优先」（读「主要内容/核心指标」判口径来提命中率）。
我们建的 panel 如果没写 description，就是给下游检索挖坑。

**description 是给业务看的**，参考飞书《香肠派对》数据看板目录的 **I 列「看板主要内容」+ J 列「核心指标」**（读取方式同第 2 节）。

格式（主要内容与核心指标**必须分行**，存进 panel 时为 `主要内容：…\n核心指标：…`）：

```
主要内容：<围绕这个 panel 的内容 + 业务目的展开，「统计/汇总…，用于…」>
核心指标：<顿号(、)分隔的业务化指标/维度名，与中文列标题对齐>
```

线上参考（直接照这个风格写）：

```
主要内容：用于监控实时/当日在线人数波动，判断是否存在大盘在线异常或活动期在线峰值变化。
核心指标：同时在线账号数、5分钟粒度在线人数趋势
```
```
主要内容：以当天新增账号作为起始人群，统计后续第N天回访比例，用于评估新增用户质量。
核心指标：新增账号基数、N日留存人数、N日留存率
```

**主要内容**要求：
- 围绕 **panel 的内容 + 业务目的**展开，句式多为「统计/汇总…，用于…」。
- ❌ **禁止埋点/技术细节**：事件名、字段名、`login_type=0`、SQL 片段、`${dt:date}` 等——业务看不懂。
- ❌ **禁止说时间窗口**：既然用动态日期参数，时间由使用者自选，描述里不提「近7天/某月/动态窗口」。
- 标准词汇用 经典派对/摸金派对/街机派对（同命名规范）。

**核心指标**要求：
- 顿号(、)分隔，写**业务化的指标/维度名**，与 SQL 的中文列标题对齐。

> 脚本 `create_panel.py` 推荐用 `--main-content` + `--core-metrics` 两个参数，自动拼成分行格式；
> 并对「主要内容」里的埋点/技术细节/时间窗口关键词做泄漏告警。

## 4. 列标题用中文（展示友好）

panel 是**实际要展示**的面板，SQL 输出的每一列都要用**中文别名**，方便看板直接读。

- 每个 select 列都给中文别名：`count(distinct pid) as "登录账号数"`、`` `#dt` as "日期" ``（中文别名用双引号）。
- **不要留英文/原始字段名当列头**（如 `pid`、`cnt`、`item_name`）。
- 列名与 description 的「核心指标」对齐。
- **格式（单位、round、百分比、小数位）先不用管** —— 由人工在看板里调整。我们只保证列名是中文、口径正确。

## 5. 建库前必校验（铁律）

**未经校验的 query 禁止建 panel。** 校验 = ds-skill 的探查 + 抽样校验流程跑通，或脚本建完
`--verify` 拉数确认能出数且数值合理。建一个错口径的常驻看板 = 长期事故。
